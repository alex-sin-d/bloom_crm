import { PlaceholderPage } from "@/components/ui/placeholder-page";

export default function EventsPage() {
  return (
    <PlaceholderPage
      eyebrow="Work"
      title="Events"
      description="Event series and annual events preserve historical years instead of overwriting them."
      sections={["Calendar placeholder", "Event table", "Annual event detail", "Venue status"]}
    />
  );
}
