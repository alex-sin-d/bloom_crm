import { PlaceholderPage } from "@/components/ui/placeholder-page";

export default function ContactsPage() {
  return (
    <PlaceholderPage
      eyebrow="Work"
      title="Contacts"
      description="Contacts distinguish named people, departmental contacts, and general organization contact routes."
      sections={["Named people", "Departmental contacts", "Contact roles", "Methods", "Activity history"]}
    />
  );
}
